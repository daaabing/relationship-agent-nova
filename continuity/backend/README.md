# Backend Scaffold

This FastAPI scaffold provides:
- `GET /health`
- `POST /api/notes/parse`

The parse endpoint currently confirms upload and returns an empty inbox so the first integration path exists before model-backed extraction is added.
